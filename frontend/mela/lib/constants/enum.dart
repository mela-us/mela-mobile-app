enum AnswerStatus{
  correct,
  incorrect,
  noAnswer,
}

enum QuitOverlayResponse{
  wait,
  quit,
  stay,
}

enum ResponseStatus{
  OK,
  SIMILAR,
  UNAUTHORIZED,
  ERROR,
  BAD_REQUEST,
  UNKNOWN
}